<amp-video id="{{video.id}}" autoplay {{#video.noaudio}}noaudio{{/video.noaudio}} {{#video.loop}}loop{{/video.loop}}
    width="0.56"
    height="1"
    layout="fill"
    poster="assets/videos/{{{video.folder}}}/poster.jpg">
    <source src="assets/videos/{{{video.folder}}}/video.webm" type="video/webm" />
    <source src="assets/videos/{{{video.folder}}}/video.mp4" type="video/mp4" />
    <track src="assets/videos/{{{video.folder}}}/subtitles.vtt" srclang="fr" default>
    <div fallback>
        <p>Votre navigateur ne supporte pas ce format vidéo.</p>
    </div>
</amp-video>